export type Transform = {
  tx: number;
  ty: number;
  rotation: number;
};

export type PaperProps = {
  colors: string[];
};

export type Props = {
  colors?: string[];
  numberOfElements?: number;
};
